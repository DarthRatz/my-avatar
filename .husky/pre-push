#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run tests and dependency checks before pushing
npm run test:ci
# Use tsconfig for analysis to avoid parsing/module issues
# Do not block the push on parser errors; report but exit 0
npx --no-install knip --no-exit-code --tsConfig tsconfig.eslint.json
